
> 目的：用最小信息闭环，锁定 launcher 的真实入口与初始化链路；为后续 Render / Asset / JobSystem 深入建立“证据锚点”。

## A. Next Steps（本周优先级从高到低）

### A1. 锁定“真实入口点”（必须完成）
- [ ] 在 VS 中启动当前的 launcher（你设为 Startup 的那个可执行目标），**断点命中的 main/WinMain 文件路径**记录到这里：
  - Entry file: `.../main.cpp` 或 `.../WinMain.cpp`
- [ ] 从入口开始，记录前 10 个关键函数的调用序列（函数名即可）：
  1) ...
  2) ...
  ...
- [ ] 复制一份“启动后立即暂停”的 **Call Stack（20 层左右）**粘贴到 `Learn/01_Bootstrap/02_CallStack.md`（或直接粘贴在本文件附录也行）。

**验收标准（DoD）**  
你能回答：`launcher.exe` 的入口文件是哪一个，以及入口后的“初始化阶段划分”大概分几段。

---

### A2. 明确“启动工程 vs 实际可执行目标”的关系（必须完成）
- [ ] 记录：VS 里启动的是哪个 `.vcxproj`（项目名）以及产物 exe 名称/输出目录。
- [ ] 如果启动工程里只有 `dependencies.cpp`（类似聚合依赖工程壳），请标注“真正产出 exe 的工程”是哪一个。

**验收标准（DoD）**  
能画出一条线：`Startup Project -> (依赖/聚合) -> Real EXE Project -> Entry File`

---

### A3. 把启动链路切成 4 段（建议完成）
把入口后的链路按阶段写成四段，每段列出关键函数/模块（不追求细节）：
1) PreInit（日志/参数/单例检查/路径准备）
2) PlatformInit（窗口/消息循环/输入）
3) EngineInit（核心系统：FS/Jobs/Render 之类）
4) RunLoop + Shutdown

**验收标准（DoD）**  
每段至少 3 个关键函数/类名，并标注它们所在文件路径。

---

## B. Open Questions（卡点清单：回答这些就能进入下一阶段）

### B1. 入口与运行形态
- Q1：真实入口点是 `main()` 还是 `WinMain()`？入口文件路径是什么？
- Q2：launcher 是否只是“启动器”，真正的编辑器/引擎在另一个模块内启动？
- Q3：启动参数（cmdline）是否影响加载路径/渲染后端/编辑器模式？

### B2. 数据与路径
- Q4：Content/Data Root 是如何确定的？（命令行/配置/注册表/相对路径推导）
- Q5：日志文件写到哪里？启动时是否会打印 data path？

### B3. 渲染与第一帧
- Q6：渲染后端默认是什么（D3D12/Vulkan/其他）？如何切换？
- Q7：第一帧的 `BeginFrame/EndFrame/Present` 分别在哪里发生？

### B4. 任务系统与线程
- Q8：JobSystem/线程池在哪里初始化？启动期会起几个线程？
- Q9：Render thread 是否独立？与主线程同步点在哪里？

---

## C. Evidence（每次提交记录时必须附带的“证据”）
- [ ] Entry file 路径（必填）
- [ ] 20 层调用栈（必填）
- [ ] 启动命令行（如果有参数）
- [ ] 启动日志片段（前 200 行即可）
- [ ] 你的结论（用“我确认/我推测”标注）

---

## D. 我（ChatGPT）拿到这些信息后会做什么
当 A1/A2 完成后，我会输出：
1) 下一周的“必读文件清单”（1~3 个文件/次，严格按调用栈推进）
2) launcher 初始化链路的模块地图（第一版）
3) 针对 B 里的问题给出“定位点/搜索关键字/断点建议”